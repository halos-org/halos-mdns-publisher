name: 'Run Tests'
description: 'No tests for transitional package'

runs:
  using: 'composite'
  steps:
    - name: No tests needed
      shell: bash
      run: echo "Transitional package — no tests to run"
