# Dockerfile for building Debian packages (transitional package, no Rust)
FROM debian:trixie-slim

RUN apt-get update && apt-get install -y \
    build-essential \
    debhelper \
    devscripts \
    fakeroot \
    lintian \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /build
